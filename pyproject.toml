[build-system]
requires = [
    "setuptools >= 49",
    "setuptools_scm[toml] >= 4",
    "wheel >= 0.35",
]
build-backend = 'setuptools.build_meta'

[tool.black]
target-version = ['py38']
line-length = 72
exclude = '''
(
    /(
        \.eggs
        \.git
        build
        dist
    )/
)
'''
